_format_version: "1.0"

add-plugins:
  - selectors:
      - "$..services[?(@.name=='bookings')]"
    overwrite: false
    plugins:
      - name: correlation-id
        enabled: true
        config:
          header_name: x-correlation-id
          generator: uuid#counter
          echo_downstream: true
